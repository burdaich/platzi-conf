<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.platzi.conf">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlatziConf">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDuk_gdcK1U533eqKA3uXXAFAGWwiz24WI" />

        <activity
            android:name=".view.ui.activities.SplashscreenActivity"
            android:theme="@style/Theme.PlatziConf.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.ui.activities.MainActivity"
            android:theme="@style/Theme.PlatziConf.NoActionBar"></activity>
    </application>

</manifest>